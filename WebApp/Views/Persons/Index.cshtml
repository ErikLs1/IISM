@model WebApp.Models.Index.ViewModel.PersonViewModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">@Base.Resources.Common.CreateNewLink</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @App.Resources.Domain.Person.PersonFirstName
            </th>
            <th>
                @App.Resources.Domain.Person.PersonLastName
            </th>
            <th>
                @App.Resources.Domain.Person.PersonPhoneNumber
            </th>
            <th>
                @App.Resources.Domain.Person.PersonAddress
            </th>
            <th>
                @App.Resources.Domain.Person.PersonGender
            </th>
            <th>
                @App.Resources.Domain.Person.PersonDateOfBirth
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Persons) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PersonFirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PersonLastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PersonPhoneNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PersonAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PersonGender)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PersonDateOfBirth)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">@Base.Resources.Common.EditLink</a> |
                <a asp-action="Details" asp-route-id="@item.Id">@Base.Resources.Common.DetailsLink</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">@Base.Resources.Common.DeleteLink</a>
            </td>
        </tr>
}
    </tbody>
</table>
